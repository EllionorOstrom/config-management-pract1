#!/bin/sh
# Задача 3: вывести текст в рамке, длина рамки зависит от длины текста

# $1 — текст, переданный как аргумент
text="$1"
# ${#text} — длина текста
len=${#text}
# создаём строку из дефисов нужной длины (len + 2 для пробелов по бокам)
line=$(printf '%*s' "$((len + 2))" '' | tr ' ' '-')

echo "+${line}+"
echo "| ${text} |"
echo "+${line}+"
